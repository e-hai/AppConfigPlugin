plugins {
      id("io.github.e-hai.config") version "1.0.0"
}


buildConfig {
    packageName = "com.demo.example"
    className = "BuildConfig"
    propertiesFile("local.properties")
    field("HOST")
    field("API_KEY")
}
